/**
 * Phase 1 stub re-exports — prefer `meet-attach.ts` (Phase 2 linked rooms).
 */
export {
  buildMeetAttach,
  buildMeetAttachStub,
  defaultMeetLinkForProject,
  meetInviteUrl,
  meetMeetingIdForEvent,
  meetMeetingIdForProjectRef,
  type MeetAttach,
  type MeetAttachStub,
} from './meet-attach.js'
