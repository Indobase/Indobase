## Learned User Preferences

- External code audit must have zero Supabase naming in the shipped bundle; fork and rebrand upstream services on GitHub/npm if needed.
- Use Indobase branding in UI, docs, and packages—not Supabase icons, logos, or "fork/clone of Supabase" language.
- Default git branch is `main`; when asked to push, ship, or deploy, commit on `main`, push to `origin main` after syncing, and roll out Studio/Builder on Vyom unless the user opts out.
- Do not commit, push, or deploy unless the user explicitly asks.
- User grants VPS SSH access for infra fixes and expects agents to work autonomously until issues are resolved.
- Product goal is Supabase Cloud parity, including multi-tenant architecture with a dedicated database per new project; dedicated/isolated tenant stacks are for paid users—free remains project-based.
- Prefer fleet-wide permanent fixes for tenant data-plane and connection issues over per-project patches.
- Product must meet India DPDP (Digital Personal Data Protection) compliance requirements.
- Free plan cannot open backend Studio—prompt upgrade to Basic+; plan ladder is Free ₹0 / Basic ₹499 / Pro ₹1,999 / Studio ₹6,999 / Enterprise custom (Pro is capacity headroom, not Studio unlock).
- Builder should offer real working starter templates wired to Indobase (`@indobaseinc/indobase-js`), not empty codegen from scratch.
- When hardening Builder, fix whole-builder pipeline issues (build/deploy/templates/chat), not single-template-only patches.
- Builder must not expose provider/model pickers in the UI; route LLMs server-side only (`/api/models` returns empty lists; hide Cloud/Local Providers settings).

## Learned Workspace Facts

- Indobase is a SaaS control plane (Studio `/api/platform/*`, `saas.*` schema, Razorpay billing) on a Supabase-compatible data plane (Kong, GoTrue, PostgREST, per-tenant stacks). Platform `/api/platform/auth/*` must use tenant-scoped GoTrue on dedicated `isolated_stack` projects, not control-plane Kong. `decryptString()` must try both `PG_META_CRYPTO_KEY` (tenant connection strings) and `CRYPTO_KEY` (project API keys).
- Published npm SDKs are `@indobaseinc/*` (`@indobaseinc/js` is the root wrapper; unscoped `indobase-js` is not owned by this org). Studio Docker/CI still imports workspace `indobase-js` until `@indobaseinc/*` is declared in `apps/studio/package.json`; undeclared `@indobaseinc/*` imports break docker-publish.
- Per-tenant data planes are Docker Compose stacks on Vyom **103.190.92.248** (provisioner `:8787`, wildcard `*.indobase.in`), provisioned via `data-plane-provisioner` with `repair-stack` / `repair-fleet` self-healing. Control-plane Studio, Builder, and marketing website on **103.190.92.249** as separate Swarm apps, not in tenant compose stacks. Do not use Hostinger `187.77.30.165` for new workloads.
- Control plane and tenant DBs share one Postgres host: keep `POSTGRES_PASSWORD` and `SAAS_DATA_PLANE_AUX_ROLE_PASSWORD` aligned; fleet `repair-stack` must ALTER ROLE `authenticator` only once cluster-wide, not per tenant. Slow `saas.grant_studio_access()` without fast-path blocks Organizations; `ensureSaasStudioDbPrivileges.ts` probes privileges first. Missing `saas.project_edge_function_secrets` breaks data-plane auto-repair.
- Studio split-deploy images may bake a demo `NEXT_PUBLIC_ANON_KEY` at build time while Kong expects production `ANON_KEY` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` at runtime. GoTrue reads anon credentials via `packages/common/public-env.ts` and `window.__INDOBASE_PUBLIC_ENV__` from `_document.tsx` (server secrets must override the demo key). Static SSR may omit `anonKey` — `_document.tsx` sync-fetches `/api/platform/runtime-public-env` before auth-js session recovery. Kong `apikey` mismatch returns "Invalid authentication credentials", not a password error. Primary buttons use gold brand HSL tokens (`hue ~43`) in `apps/studio/styles/main.scss`.
- Legacy Model A projects (shared control-plane DB) cannot list `auth.users` until a dedicated tenant DB exists; Studio auto-provisions on Auth Users by default (opt out with `SAAS_AUTO_PROVISION_DEDICATED_ON_AUTH_USERS=false`). Project detail exposes `hasDedicatedDatabase`.
- Tenant stack health depends on `tenant-functions` volume `main/index.ts` (provisioner seeds on repair); missing stub crash-loops edge runtime. Provisioner `pingHttp` must treat GoTrue HEAD `/health` 405 as healthy when GET succeeds.
- Builder lives in monorepo `indobase-builder/` (Remix); standalone GitHub `Indobase/builder` mirrors it for Swarm deploys. Studio handoff via `/launch` + matching `BUILDER_HANDOFF_SECRET` (production requires explicit secret); production `builder.indobase.in` (Swarm on `.249`). Default chat provider must be OpenRouter (matches `DEFAULT_MODEL`), not OpenAI — OpenAI fallback causes submit auth errors. Server-side LLM routing in `openrouter-model-policy.ts`: discuss/planning/context use OpenRouter free models; codegen and autonomous repair use `deepseek/deepseek-v4-flash`. Free orgs get 5 prompts (build and discuss) via `saas.organizations.builder_prompts_used` and Studio `/api/platform/projects/[ref]/builder/prompt-quota`; planner orchestration runs for all build-mode chats. Bundled Indobase templates in `indobase-builder/templates/` via `/api/local-template`; `finalizeCodegen()` after each chat response. One-click deploy: Studio `publishDeploymentArtifacts` must finalize sync Builder uploads as `ready` with `skipInlineProcessing`; Builder `publishToIndobase` calls `ensureBuilderSession()` before upload. WebContainer preview needs Vite on port 5173 with `host: true`. Builder header/deploy: use nanostore atoms (`filesCountAtom`) with `useStore`, never scalar getters like `filesCount`; `DeployButton` must declare `isDeploying` via `useState`; guard script `scripts/check-nanostore-usestore.mjs` runs in Builder tests.
- VPS data-plane ops: Traefik must stay on compose backend network (`indobase-traefik-attach-compose-network.sh`); `cap-idle-tenant-stacks.sh` pauses excess stacks (HTTPS timeout until wake/repair—avoid duplicate cron on `.248`). Provisioner `readPublishedPort` must match `172.17.0.1` bindings, not only `0.0.0.0`. Provisioner `docker compose -f` must use host tenant paths (`/var/lib/indobase/tenants`), not container `/mnt/tenants`, for bind mounts. Static hosting is storage-first: shared site proxy `:8790` + `site-routes.json` via `/register-site-route`; per-tenant nginx `publish-site` is fallback only (`SITE_STATIC_PROXY_ENABLED` on `.248`). CI image `roshanraghavander/ind-repo-provisioner:<sha>`.
- Production Studio/Builder deploy: GitHub `Indobase/Indobase` `main` → `.github/workflows/docker-publish.yml` builds both `roshanraghavander/ind-repo:<git-sha>` and `roshanraghavander/indobase-builder:<sha>` → Vyom control-plane VPS **103.190.92.249** (Docker Swarm); verify Studio via `studio.indobase.in/api/health/live` `version`; run `indobase-studio-attach-compose-network.sh` after Studio update. Marketing site `indobase.in` is Swarm service `indobase-website` on `.249` (`roshanraghavander/indobase-website:<sha>`), not Hostinger; `apps/www/Dockerfile` must build from monorepo root because `hooks.server.ts` imports `packages/common/studio-docs-path-map.json`.
- New projects get unique per-project JWT secrets. Existing shared-secret projects require the intentionally breaking `POST /api/cron/backfill-project-jwt-secrets` rotation, which is dry-run by default; `saas.organization_members` has a composite PK (`organization_id`, `gotrue_id`) and no `id`, so deterministic scans order by `inserted_at` then `gotrue_id`.
- Production hosts: `indobase.in` (marketing), `studio.indobase.in`, `api.indobase.in`, `builder.indobase.in`, and per-project tenant endpoints at `[project-ref].indobase.in`. Engineering docs: `docs/ENGINEERING-PLATFORM-HANDBOOK.md`, `docs/TEAM-HANDBOOK.md`. Phase A audit rebrand: `docs/AUDIT_REBRAND.md`, `scripts/audit-no-supabase.sh`.
