/** @deprecated Prefer `../documents` — Gen-1 path. */
export type {
  DesignDocumentRef,
  DesignCommandKind,
  DesignDocumentContract,
  DesignDocument,
} from '../documents'
export { designToDocumentRef, createDesignDocument } from '../documents'
