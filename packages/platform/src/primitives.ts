/**
 * @deprecated Import from `./commands` — kept so older relative imports keep working.
 */
export type { Command as PlatformCommand, PlatformIntent } from './commands'
export type { CapabilityContract } from './contracts/capability-contract'
