/**
 * Backend connect UI removed for non-technical users.
 * Studio SSO handoff still links Indobase automatically.
 * Otherwise Builder agents auto-provision Indobase backend — no URL/key forms.
 */
export function BackendLinkBanner(_props: { className?: string }) {
  return null;
}
